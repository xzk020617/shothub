# 截图中转站 · 技术方案

> 目标：Windows 自用工具。启动/后台运行时自动保存截图工具产生的截图，支持手动添加/拖入，缩略图展示，可复制粘贴或拖出使用；退出时清理未保存截图，支持单张删除与一键清空。

## 0. 技术选型

**假设**：自用、追求开发速度、不追求商店级品质。

- **推荐：Python + PySide6 + pywin32**
  - 剪贴板监听（`AddClipboardFormatListener` / `WM_CLIPBOARDUPDATE`）、系统托盘、全局热键、拖放全部成熟可用
  - PyInstaller 打包为单 exe
- 备选：C# WPF（体验最原生但慢）、Electron（不推荐，内存大且剪贴板监听需原生模块）
- 下文均按 PySide6 方案描述

## 1. 用户流程

**主流程（自动捕获）**
1. 启动程序 → 主窗口 + 托盘图标，开始监听剪贴板
2. 用户用任意截图工具截图（Win+Shift+S 等）→ 图片进入剪贴板 → 程序检测并落盘 → 列表顶部出现新缩略图
3. 取用：
   - 路径 A：点击缩略图"复制"（或双击）→ 写入剪贴板 → 目标软件 Ctrl+V
   - 路径 B：直接把缩略图拖出窗口 → 以文件形式拖入目标
4. 关闭窗口 → 最小化到托盘继续监听（**假设**：关窗口 ≠ 退出，托盘右键才是真退出）
5. 退出程序 → 清除未保留截图缓存 → 进程结束

**手动添加**：拖文件进窗口 / Ctrl+V 粘贴 / "+"按钮选文件
**删除**：缩略图 hover "×" 删单张；工具栏"清空全部"（二次确认）

## 2. 功能清单

### MVP 必做
| 功能 | 说明 |
|---|---|
| 剪贴板图片监听 | 监听 `WM_CLIPBOARDUPDATE`，识别 DIB/BMP/PNG，落盘为 PNG |
| 自身写入过滤 | 自己"复制出去"不触发再次入库（防死循环） |
| 缩略图网格列表 | 新图在上，时间戳/尺寸，懒加载 |
| 复制出去 | 单击/双击/按钮，原图写回剪贴板 |
| 拖入 | 文件拖入 + Ctrl+V 粘贴 |
| 拖出 | 缩略图可拖拽为文件（缓存文件真实存在，天然支持 CF_HDROP） |
| 单张删除 / 一键清空 | 清空需二次确认 |
| 托盘常驻 | 最小化到托盘，托盘菜单（打开/清空/退出） |
| 退出清理 | 退出时删除缓存目录内未保留文件 |
| 单实例运行 | 重复启动唤起已有窗口 |

### 可选增强
- 置顶小窗 / 全局热键呼出
- "保留"(pin) 标记：pinned 图退出不清理
- 容量上限（如 50 张 / 500MB），FIFO 淘汰
- 按 hash 去重
- OCR 搜索
- 开机自启（注册表 Run 键）

### 暂不做
- 云同步 / 多设备
- 账号体系
- 图片编辑（标注、裁剪）
- 识别"截图完成事件"（Windows 无统一 API，只能靠剪贴板）

## 3. 页面/组件

```
MainWindow
├── TopBar
│   ├── 标题 + 计数（"12 张 · 86MB"）
│   ├── 「添加」按钮
│   └── 「清空全部」按钮
├── ThumbnailGrid（QListWidget / 自绘 FlowLayout）
│   └── ThumbnailCard × N
│       ├── 缩略图（异步生成，缓存）
│       ├── 时间戳 + 尺寸
│       ├── hover 浮层：复制 / 删除
│       └── 支持拖出（drag source）与多选
├── DropOverlay（拖入时全窗口高亮提示层）
├── EmptyState（空状态提示"截图后自动出现在这里"）
├── TrayIcon（托盘图标 + 右键菜单）
└── ConfirmDialog（清空/退出确认弹窗）
```

## 4. API / 数据结构

自用单机程序，无网络 API，内部模块接口：

```
ClipboardMonitor   → onImageCaptured(imageBytes)      # 监听→回调
StorageManager     → save(bytes)->Item / delete(id) / clear() / cleanupOnExit()
                     list()->[Item] / totalSize()
ThumbnailCache     → get(itemId, size)->QPixmap       # 内存LRU + 磁盘缩略图
ClipboardWriter    → putImage(path)                   # 复制出去（带自我标记）
DragDropHandler    → 拖入校验 / 拖出构造 mimeData
```

**数据结构**（`manifest.json` 持久化，防崩溃丢列表）

```json
{
  "items": [{
    "id": "uuid",
    "filePath": "cache/2026-08-30_200312_ab12.png",
    "thumbPath": "cache/thumbs/ab12_256.png",
    "createdAt": "2026-08-30T20:03:12",
    "source": "clipboard | dragin | paste | picker",
    "pinned": false,
    "width": 1920, "height": 1080, "bytes": 345112,
    "status": "active"
  }]
}
```

**状态流转**
```
[剪贴板/拖入捕获] → active ──复制/拖出──→ active（不变，出库不删）
                      ├──删除──→ deleted（删文件+移出 manifest）
                      └──程序退出──→ 若 pinned=false → deleted
```

**自我写入过滤**：写剪贴板时记录 `GetClipboardSequenceNumber()` 或设置时间戳标记，监听器比对后跳过自身写入。

## 5. 异常与边界

| 场景 | 处理 |
|---|---|
| 空状态 | EmptyState 提示；清空按钮置灰 |
| 剪贴板非图片 | 静默忽略，不入库不报错 |
| 超大截图（8K 多屏） | 原图不限，缩略图最长边 512px；**假设**：单张 >100MB 给提示但不拒绝 |
| 缓存目录无写权限/磁盘满 | 启动自检写权限；写入失败一次性提示并暂停入库，不崩 |
| 重复提交（同图连截） | MVP 照常存两张；增强版 hash 去重 |
| 复制格式不兼容 | 同时写 CF_DIB + PNG 两种剪贴板格式（兼容微信/PS/Office） |
| 并发 | 监听线程→队列投递→UI 线程落盘刷列表；删图/入库同文件按 id 加锁 |
| 双开 | named mutex 单实例锁，二次启动唤起已有窗口 |
| 异常退出（崩溃/断电） | 下次启动扫描缓存目录，按 manifest 清理残留 |
| 拖入不支持文件 | DropOverlay 提示"仅支持图片"，drop 时过滤后缀 |
| 退出清理失败（文件占用） | 跳过该文件，留给下次启动清理，不阻塞退出 |

## 6. 开发顺序（4 步到可用）

1. **存储 + 列表骨架**：StorageManager + manifest + 缩略图网格 + 手动"添加" + 单张删除
2. **剪贴板闭环**：监听器入库 + 自我过滤 + 复制出去（核心价值打通）
3. **拖拽双向**：拖入（含 Ctrl+V）+ 拖出为文件
4. **常驻与清理**：托盘 + 单实例 + 退出清理 + 崩溃残留清理 + 空状态打磨

（可选第 5 步：热键呼出、pin 保留、容量上限、开机自启）

## 7. 验收标准

- **第 1 步**：添加 10 张不同尺寸图 → 缩略图和时间正确显示；删除 → 列表和磁盘文件同时消失；杀进程重启 → 列表仍在（manifest 生效）
- **第 2 步**：后台运行时 Win+Shift+S 连截 3 张 → 全部按序出现在列表顶部；点"复制"→ 可粘贴进微信和 Word；复制出去不会导致重复入库
- **第 3 步**：从资源管理器拖 3 张图进窗口 → 入库成功；从窗口拖一张到桌面 → 得到可用 PNG；拖 .txt → 被拒绝且有提示
- **第 4 步**：关窗口后托盘仍在且继续捕获；托盘退出 → 缓存目录清空；重复双击 exe → 只唤起已有窗口；taskkill 强杀后重启 → 无残留堆积
- **第 5 步（追加迭代）**：截图工具里连续编辑（每步都会重写剪贴板）→ 中转站只保留最终版，中间版本原位被替换；点「📌 置顶」→ 窗口悬浮在其他窗口之上，再点取消

## 追加迭代（2026-08-31）

1. **编辑合并**：截图工具编辑时每步都会把新图写进剪贴板。捕获到新图时，若上一条目是自动捕获、未置顶、尺寸一致、180 秒内且内容高相似（64×64 下采样，差异像素 ≤40%），则原位替换旧图（`replace_image`），卡片原地刷新——关掉编辑器时只留最终版。手动添加/拖入/粘贴的条目不参与合并。
2. **窗口置顶**：顶栏新增「📌 置顶」开关，切换 `WindowStaysOnTopHint`。

## 工作量估计

MVP（前 3 步）约 1-2 天，完整 4 步约 2-3 天（业余开发计）。
